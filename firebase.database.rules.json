{
  "rules": {
    "rooms": {
      "$roomId": {
        ".read": true,
        ".write": true,
        ".validate": "newData.hasChildren(['state', 'updatedAt']) || !newData.exists()"
      }
    }
  }
}
